{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "yarn --cwd packages/backend start"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
